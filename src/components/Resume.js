import React, { Component } from 'react';


class Resume extends Component {
  render() {
    const { type } = this.props;
    return (
      <div id={type} key="resume_section">
      
      </div>
    );
  }
}

export default Resume;
